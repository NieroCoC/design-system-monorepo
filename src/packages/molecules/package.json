{
  "name": "@mskcc/molecules",
  "version": "0.0.1",
  "description": "These are molecule components for mskcc",
  "author": "Camila Coder <nierococ@mskcc.org>",
  "license": "MIT",
  "private": true,
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0"
  },
  "scripts": {
    "clean": "rm -rf ./lib",
    "prebuild": "yarn clean",
    "build": "tsc --build \"./tsconfig.json\"",
    "postbuild": "cp -r ../../assets/css lib/css && rm ./lib/css/compiled.css"
  }
}
